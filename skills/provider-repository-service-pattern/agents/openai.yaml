interface:
  display_name: "Project Organization"
  short_description: "Use the repo project and layering organization rules."
  default_prompt: "Use $provider-repository-service-pattern to place this code in the correct project and layer."
