interface:
  display_name: "Augustus Logger Practices"
  short_description: "Use Augustus logging correctly in Coacher."
  default_prompt: "Apply the repo rules for IAugustusLogger, structured payloads, and exception logging."
