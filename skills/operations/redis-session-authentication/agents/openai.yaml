interface:
  display_name: "Redis Session Auth"
  short_description: "Use Redis session auth in Coucher."
  default_prompt: "Use $redis-session-authentication to add or update Redis-backed session authentication for REST and GraphQL in Coucher."
